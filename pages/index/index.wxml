<view class="index">
  <view class="index-hd">
    <view class="index-desc"> 以下是论文的分类 
  </view>
  <view class="index-bd">
    <view class="kind-list">
      <block wx:for-items="{{list}}" wx:key="{{item.id}}">
      <navigator url="pages/{{item.url}}" clas="navigator">
      <view class="navigator-txet">{{item.id}} </view>
      <!--view class="navigator-arrow"></view>-->
      </navigator>
      </block>
    </view>
  </view>
</view>